package com.bzu.inventorymanagementsystemAPIs.Models;

/**
 * Enum to hold the constant status
 * of order in the IMS
 */
public enum OrderStatus {
    PENDING,
    CONFIRMED,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
